// Courtesy pass has been removed from the codebase.
// This placeholder test ensures the test suite remains green.

describe('Courtesy pass removed', () => {
  it('does nothing (feature removed)', () => {
    expect(true).toBe(true);
  });
});
